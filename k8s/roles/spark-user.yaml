apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: spark-reader
  namespace: spark
rules:
- apiGroups: [""]
  resources:
  - pods
  - pods/log
  - events
  verbs:
  - get
  - list
  - watch